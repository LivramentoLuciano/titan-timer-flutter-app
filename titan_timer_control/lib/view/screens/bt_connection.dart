import 'package:flutter/material.dart';

class BtConnectionScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Conectar al Bluetooth")),
      body: Center(child: Text("Bluetooth Connection Screen")),
    );
  }
}